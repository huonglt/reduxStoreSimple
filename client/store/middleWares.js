export const logging1 = (action, state) => {
  console.log('logging1: state = ', state, ', action = ', action);
  return state;
};

export const logging2 = (action, state) => {
  console.log('logging2: state = ', state, ', action = ', action);
  return state;
};
